import React from 'react';
import ReactDOM from 'react-dom';
import Hero from './Song';

it('renders without crashing', () => {
  const boxsongs = document.createElement('boxsongs');
  ReactDOM.render(<App />, div);
});
