import React from 'react';
import ReactDOM from 'react-dom';
import Hero from './Video';

it('renders without crashing', () => {
  const boxsongs = document.createElement('boxvideos');
  ReactDOM.render(<App />, div);
});
